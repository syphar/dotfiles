#!/usr/bin/env bash
# Start from the repository root.
cd ./$(git rev-parse --show-cdup)

# update ctags for project
git ls-files . --cached --exclude-standard --others | ctags -L - 2> /dev/null > /dev/null
